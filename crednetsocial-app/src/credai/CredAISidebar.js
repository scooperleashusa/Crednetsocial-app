import React from "react";
import SymbolicName from "../identity/SymbolicName";
import BreadcrumbScore from "../identity/BreadcrumbScore";

const CredAISidebar = () => (
  <aside className="credai-sidebar">
      <SymbolicName name="§(Evuro)" />
          <BreadcrumbScore score={72} />
              <p>Tomorrow: live signals from Firestore.</p>
                </aside>
                );

                export default CredAISidebar;