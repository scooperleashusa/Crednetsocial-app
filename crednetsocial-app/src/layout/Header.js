import React from "react";

const Header = () => (
  <header className="app-header">
      <h1>CredNetSocial</h1>
          <p>Symbolic, reputation-aware social for §(name).</p>
            </header>
            );

            export default Header;