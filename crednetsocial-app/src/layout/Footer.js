import React from "react";

const Footer = () => (
  <footer className="app-footer">
      <small>© {new Date().getFullYear()} CredNetSocial · EVURO</small>
        </footer>
        );

        export default Footer;