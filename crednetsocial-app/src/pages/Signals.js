import React from "react";
import SignalsFeed from "../identity/SignalsFeed";

const Signals = () => (
  <section>
      <h2>Signals</h2>
          <SignalsFeed />
            </section>
            );

            export default Signals;