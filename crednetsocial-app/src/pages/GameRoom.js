import React from "react";

const GameRoom = () => (
  <section>
      <h2>Game Room</h2>
          <p>Human-to-human chat. Tomorrow: real-time rooms + signals.</p>
            </section>
            );

            export default GameRoom;