import React from "react";
import SymbolicName from "../identity/SymbolicName";

const Profile = () => (
  <section>
      <h2>Profile</h2>
          <SymbolicName name="§(Evuro)" />
              <p>Tomorrow: editable identity, linked wallets, and history.</p>
                </section>
                );

                export default Profile;