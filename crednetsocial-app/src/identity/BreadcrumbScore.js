import React from "react";

const BreadcrumbScore = ({ score }) => (
  <div className="breadcrumb-score">
      <h3>Breadcrumb Score</h3>
          <p>{score}</p>
            </div>
            );

            export default BreadcrumbScore;