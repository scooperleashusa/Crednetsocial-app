import React from "react";

const SignalsFeed = () => (
  <div className="signals-feed">
      <p>Tomorrow: live Firestore-driven signals stream.</p>
        </div>
        );

        export default SignalsFeed;